import React from 'react';
import { Link, Switch, useRouteMatch, useParams, Route } from 'react-router-dom';

const SinglePortfolio = () => {
    // let match = useRouteMatch();

    return (
        <>
            <h1>Single Portfolio</h1>
        {/* <Link to={`/swaap`}>
        </Link>

        <Switch>
            <Route path={'/swaap'}>
                <SinglePortfolio />
            </Route>
        </Switch> */}
        </>
    )
}

export default SinglePortfolio